import { LoadingOverlayDirective } from './loading-overlay.directive';

describe('LoadingOverlayDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadingOverlayDirective();
    expect(directive).toBeTruthy();
  });
});
