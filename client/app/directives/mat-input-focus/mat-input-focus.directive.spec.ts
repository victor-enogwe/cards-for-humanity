import { MatInputFocusDirective } from '../mat-input-focus.directive';

describe('MatInputFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new MatInputFocusDirective();
    expect(directive).toBeTruthy();
  });
});
